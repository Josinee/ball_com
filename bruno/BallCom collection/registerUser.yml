info:
  name: registerUser
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/customers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
