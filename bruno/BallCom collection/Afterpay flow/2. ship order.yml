info:
  name: 2. ship order
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/shipments/{{orderIdAfterPay}}/ship
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
