info:
  name: get catalog by id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/catalogs/{{catalogId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
