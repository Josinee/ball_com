info:
  name: import CSV
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8081/api/customers/import
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
