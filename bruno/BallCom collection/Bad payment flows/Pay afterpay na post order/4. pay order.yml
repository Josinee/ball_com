info:
  name: 4. pay order
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/payments/pay/{{orderIdAfterPayBad}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
