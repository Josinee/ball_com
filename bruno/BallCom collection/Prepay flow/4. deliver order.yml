info:
  name: 4. deliver order
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/shipments/{{orderIdPrePay}}/deliver
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
