info:
  name: 3. ship order
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/shipments/{{orderIdPrePay}}/ship
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
