info:
  name: 1. post order
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/orders/placeorder
  body:
    type: json
    data: |-
      {
        "customerId": "{{customerId}}",
        "items": [
          {
            "productId": "99999999-ffff-ffff-ffff-999999999999",
            "quantity": 2,
            "unitPrice": 12.50
          },
          {
            "productId": "88888888-eeee-eeee-eeee-888888888888",
            "quantity": 1,
            "unitPrice": 45.00
          }
        ],
        "paymentMethod": "PREPAY"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        if (data && data.orderId) {
          bru.setEnvVar("orderIdPrePay", data.orderId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
