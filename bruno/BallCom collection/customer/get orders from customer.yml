info:
  name: get orders from customer
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/orders/customer/{{customerId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
